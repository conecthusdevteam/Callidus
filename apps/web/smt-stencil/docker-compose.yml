services:
  smt-stencil-web:
    container_name: ${NODE_ENV}-smt-stencil-web
    build:
      context: .
      args:
        VITE_API_URL: ${VITE_API_URL}
    ports:
      - "${WEB_PORT:-5173}:81"
    restart: unless-stopped
