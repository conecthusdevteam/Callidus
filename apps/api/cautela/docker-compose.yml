services:
  api:
    build:
      context: .
      args:
        PORT: ${PORT}
    container_name: ${NODE_ENV}-cautela-api
    env_file:
      - .env
    ports:
      - '${PORT:-3000}:3000'
    environment:
      - PORT=3000
      - CLIENT_PORT=${CLIENT_PORT}
    volumes:
      - .:/app
      - /app/node_modules
    command: npm run start:dev
    depends_on:
      - sqlserver

  seed:
    build: .
    container_name: ${NODE_ENV}-cautela-seed
    env_file:
      - .env
    profiles:
      - seed
    volumes:
      - .:/app
      - /app/node_modules
    command: npm run seed
    depends_on:
      - sqlserver

  migrate:
    build: .
    container_name: ${NODE_ENV}-cautela-migrate
    env_file:
      - .env
    profiles:
      - migrate
    volumes:
      - .:/app
      - /app/node_modules
    command: npm run migration:run
    depends_on:
      - sqlserver

  sqlserver:
    image: mcr.microsoft.com/mssql/server:2022-latest
    container_name: ${NODE_ENV}-cautela-sqlserver
    ports:
      - '${DB_HOST_PORT}:1433'
    environment:
      ACCEPT_EULA: 'Y'
      SA_PASSWORD: 'SecurePassword123!'
      MSSQL_PID: 'Developer'
    volumes:
      - sqlserver_data:/var/opt/mssql

volumes:
  sqlserver_data:
