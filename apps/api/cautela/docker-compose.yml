services:
  api:
    build: .
    container_name: cautela-api
    env_file:
      - .env
    ports:
      - "${PORT:-3000}:3000"
    volumes:
      - .:/app
      - /app/node_modules
    command: npm run start:dev
